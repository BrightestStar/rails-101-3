<h2>新增讨论版</h2>

<%= simple_form_for @group do |f| %>

<div class="form-group">
    <%= f.input :title %>
    <%= f.input :description %>
</div>

<%= f.submit "Submit", data: { disable_with: "Submitting..." }%>
<% end %>
